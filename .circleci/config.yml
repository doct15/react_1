workflows:
  my-workflow:
    jobs:
      - build:
          context:
            - build-env-vars
            - docker-hub-creds

jobs:
  build:
    docker:
      - image: doct15/react1:ci2
        auth:
          username: doct15
          password: $DOCKERHUB_PASSWORD
